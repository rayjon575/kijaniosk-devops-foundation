# Cloud Service Model

The KijaniKiosk platform will primarily use **PaaS (Platform as a Service)**:

- Reduces operational overhead for VMs and OS maintenance
- Allows developers to focus on application logic
- Scales automatically with demand
- Managed services for databases, caching, and load balancing

IaaS could be used for specific custom infrastructure, but PaaS is preferred for speed and reliability.